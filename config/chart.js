// const { CanvasRenderService } = require('chartjs-node-canvas'); // Include chartjs-node-canvas
// const chartConfig = require('./chartConfig'); // Import chart configuration


// // Function to render the chart
// async function renderChart() {
//     const canvasRenderService = new CanvasRenderService(800, 600);
//     return await canvasRenderService.renderToBuffer(chartConfig);
// }